# 产品策略汇总（PRODUCT_STRATEGY）

> 本文档汇总了产品架构、成本分配、部署与定价的核心决策讨论，作为后续落地的参考基线。

## 一、成本与责任分配（服务器托管模式）

- **模型 / 生图 / 生视频 API 费**：用户付（BYOK 自带 key 或套餐充值）→ 最大成本已转移给用户。
- **运营方（你）仍承担**：固定服务器（CPU / 内存 / DB / 队列 / SSE）+ 边际成本（编排算力、带宽、存储）+ 若本地跑 GPU 则最贵的视频算力。
- **用户侧承担**：模型费、等待 / 排队、联网与可用性依赖、隐私顾虑、配额约束。
- **核心矛盾**：模型费转移了，但"流水线在你服务器跑"的基建成本没转移 → 纯中心化下你的压力 = 固定成本 + 随用户量线性涨的边际成本 + 峰值 / 账单失控风险。

## 二、桌面端 coding agent 的算力模型（参照）

- 非 LLM 计算 / 渲染在用户本机；但**喂给 LLM 的上下文（代码 / 命令输出）和会话记忆会出本机**。
- 例外：ChatGPT 云端版 Codex 代码执行在 OpenAI 云，非用户本机。
- 启示：客户端分担算力 = 把除 LLM 推理外的一切按在用户电脑。

## 三、TapNow 为什么纯 web 中心化（竞品验证）

- 内容生产场景天然中心化：异步长任务、用户没 GPU、产物要沉淀成平台资产。
- 用 Hailuo / Veo / Wan 等第三方云端 API 把最贵 GPU 甩出去，自己只扛编排 + 存储 + 带宽。
- 优势：零安装获客、算力全局优化、资产沉淀 = 留存护城河、商业模型干净。

## 四、现阶段是否只做 web 端（核心结论）

- **结论：只做 web 端完全可行，且是正确选择。** 别被桌面 coding agent 带偏——那是不同场景（必须碰用户本地代码）。
- 唯一真代价是服务器算力账单，但 BYOK + S3 + budget 已能控在边界内。
- **落地优先级**：
  - P0 修破图让产物能看、核心闭环跑通
  - P1 budget 硬上限 + 队列削峰 + cost 计量钉死成本边界
  - P2 图库 / 模板资产沉淀做留存
  - P3 商业化收口（BYOK / 套餐、视频限次）
- 桌面端触发条件：cost 显示重度用户边际亏损 / 重度本地创作需求 / 获客触顶。

## 五、图文产物存哪、用户怎么拿

- **存 S3**（已有 `STORAGE_BACKEND=s3`），DB 只存 `artifact_id` / key，不塞二进制、不存模型外链。
- 前端统一走 `/api/artifacts/:id`（后端从 S3 取）→ 直接治掉破图根因（之前 `extractArtifacts` 把外链原样存 uri）。
- 用户获取：画廊查看 / 下载（单张 + 批量 zip）/ 签名链接分享（别给 S3 公链）/ 当模板复用 / 未来对接发布。
- 边界：按 `user_id` 隔离鉴权；S3 生命周期 + 套餐配额控存储膨胀。

## 六、发布到公网 Cloudflare / Vercel

- **Vercel 不能扛整个产品**：函数超时、无状态、无持久 worker，跑不了长任务 / 队列。
- **分层部署**：前端 + 薄 API 上边缘（Vercel / Cloudflare Pages / Workers）；重引擎 worker + 队列 + 长任务放常驻容器（Fly.io / Render / 你 VM）；存储 R2 / S3；DB 外置（D1 / Neon / Supabase）。
- 最快路径：早期一台常驻 VM 全跑 + CF Pages 托管前端，验证完再拆。
- 坑：免费 plan 超时限流、跨域 / SSE 别塞 serverless、存储一致性防"图存了元数据没落"。

## 七、游戏化 RTS 视角（帝国时代 3 / 红警）

- **纯 web 可做**：Three.js 等距 3D（宏观上帝视角）+ Canvas 2D 画布（微观编辑，已有 FBHflow 基础）+ WebSocket 推真实状态。
- 机制：服务器算真实业务，前端在用户浏览器本地渲染动画 → 渲染压力在用户 GPU，不烧你服务器。
- **游戏本地运行、数据在服务器**：浏览器就是"本地客户端"，和帝国时代 3 同构。
- 节奏：属 P3 后差异化留存手段，先做 P0–P2，别抢 MVP。

## 八、定价策略（订阅能否覆盖成本）

- **能覆盖，前提是先有真实成本数据 + 最贵项 BYOK / 限次。**
- 单位经济公式：`订阅价 ≥ 单位成本 ÷ (1 − 目标毛利率)`。
- **以 BYOK 为主**：用户带自己 key，你只收平台费，成本极低可预测；纯代付重度视频用户会亏（他 API 费 $50 只付你 $10）。
- **模型分层（2026-08-30 决策，详见 product-vision-discussion §九）**：内置模型走**订阅制**——用户只能只读选择，不可改 baseUrl/apiKey/单价，订阅 gate 控制访问；**用户自定义模型**完全自主（增删改），按用户自己的 key 计实际消耗。模板对模型的引用指向内置模型命名空间，用户无需也无法配置。落地前内置真实模型不向普通用户暴露（避免免费白嫖 LLM 成本），阶段 5 前用真实模型走自定义入口。
- 三档：免费层（转化，成本≈0）/ 入门（轻度）/ 专业（覆盖成本 + 毛利）/ 团队企业（高价摊薄固定）。
- `budget` 硬上限兜底防单个用户烧穿。
- **当务之急**：先开 `cost` 计量跑 2–4 周，拿到按用户 / 运行 / 模型拆分的真实月成本，再定价。

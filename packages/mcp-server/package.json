{
  "name": "@agent-world/mcp-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-world-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@agent-world/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}

# Agent World —— 产品路线图

游戏化的多智能体编排产品。每个 Agent 是一座厂房，token 是电力，产出通过管道和卡车在厂房间流动，skill 是可装备的技能卡。

**配套文档：**
- [产品愿景讨论](docs/product-vision-discussion.md) — 能力边界、设计语言、技术选型、商业化
- [技术方案](docs/technical-design.md) — 架构、数据模型、API、前端结构
- [任务路线图](docs/roadmap-tasks.md) — 每阶段的具体任务拆解和退出条件

本文是分阶段路线图，只写每阶段的目标、范围、退出条件。设计决策的理由在 [README.md](README.md)，当前代码状态在 [handoff.md](handoff.md)。

## 产品形态的演进

三种形态都是目标，但有先后：

1. **自己和小团队用**（现阶段）—— 本地单机，无登录、无多人、无计费。只追求一件事：好用到自己每天愿意打开。
2. **开源项目**（其后）—— 目标是吸引使用者和贡献者，所以要有上手门槛、文档、示例产线、清晰的扩展点。
3. **商业产品**（最终）—— 多租户、鉴权、真实计费与配额、云端部署、团队协作。

**现阶段的取舍原则：** 凡是只为形态 2、3 服务的东西，现在一律不做，但**不要做出会堵死它们的设计**。具体来说，worker 保持可替换、事件流保持带版本、成本计量保持真实（哪怕现在不收钱），这三条是通往后两个形态的路，不能拆。

反过来，账号体系、权限模型、订阅套餐、协作光标这些，现阶段一行都不写。

## 架构护栏

以下决策贯穿所有阶段，做任何功能时不能违反：

1. **事件流是唯一真相源。** 运行时状态是事件的纯函数 fold，live 和 replay 用同一个 reducer。不引入旁路状态。
2. **节点类型固定。** source / agent / gate / sink，未来可能加 join。变化通过 skill（装备卡）承载，不开放节点类型插件。
3. **三个扩展接缝。** Worker（谁来干活，已有）、Connector（原料从哪来，阶段4）、MemoryBackend（档案怎么存，阶段5）。除此之外不做通用插件系统。
4. **Attempt 是身份。** `(runId, nodeId, attempt)` 是主键，产出不被覆盖。重试（技术故障）不增加 attempt，返工（质检驳回）才增加。
5. **成本事后计量。** 不预扣，每次调用后累加，预算是硬上限。
6. **工厂隐喻是本体论。** 每个技术概念必须能在工厂里找到对应，找不到的不暴露给用户。
7. **技能树不是付费墙。** skill 是能力开关，不设解锁成本。

## 数据模型分层（逐步引入）

管道里的数据随阶段演进，但方向已定：

- **阶段 0-1：** 管道里是字符串，`node.finished.output: string`。够用。
- **阶段 3：** 引入 Artifact，产出可以是文件/图片/结构化数据，不只文本。
- **阶段 5：** 引入 Knowledge，从历史事件流中提炼可检索的知识。向量数据库只是 Knowledge 的存储后端之一。

不提前做分层，但事件 schema 的可选字段（artifactId、metadata）在阶段 1 预留。

## 一条贯穿始终的红线

**技能树不做成付费墙。** skill 是节点上的能力开关和配置预设，不设解锁成本。花 token 解锁技能这件事，要么是真障碍会惹烦用户，要么是纯装饰会被一眼看穿 —— 两种都不做。游戏化体现在质感和隐喻上，不体现在人为的进度阻塞上。

## 阶段 0 —— 骨架与真实执行引擎（已完成）

**目标：** 先证明"搭产线 → 按下派发 → 看着它跑"这个循环本身是否成立，用假 worker 把全链路打通。

**已交付：**

- 图模型、编译器、事件流 schema、纯函数 reducer（`packages/core`，零依赖，两端共用）
- 执行引擎、worker 接缝、SQLite 持久化、HTTP + SSE（`packages/server`）
- 工业风工作台：厂房、管道、卡车、控制面板、回放拖条（`apps/web`）
- 两个硬问题落地成代码：返工环作为受约束的构造（而非任意有环图）、attempt 作为运行身份的一部分（而非可覆盖的计数器）
- 电力事后计量 + 预算上限跳闸
- 19 个测试，浏览器验证过完整循环

**退出条件（已达成）：** 假 worker 下能完整跑通"投料 → 加工 → 质检驳回 → 返工 → 通过 → 出厂"，且回放能把这个过程倒回去看。

## 阶段 1 —— 真能干活（已完成）

**目标：** 从沙盘变成工具。这一阶段结束时，我自己应该愿意用它跑真实任务，而不是只用来演示。

**已交付（2026-08-25）：** 真实模型接入（OpenAI 兼容 provider，已验证 Agnes `agnes-2.0-flash`），流式文本与思考过程、用量计量、失败分类与指数退避重试、超时控制、质检标准生效（criterion 输入框 + judge 读取）、prompt 自动保存、halt 恢复、SSE 心跳与断线重连、错误脱敏、返工原因回喂、派发时输入真实原料、token/电费双模式电表、设置面板（密钥与单价）、多模态计费模型（文本按 token、图片按张、视频按秒、音频按秒/千字符，价格字段随模型类型切换；非文本模型的运行时执行留待阶段 4）。

**范围：** 只做"让真实模型调用可用"这一件事，不扩展产线表达力。

**任务：**

- **真实 provider worker。** `packages/server/src/worker.ts` 是接缝，引擎只认这个接口。下游的流式文本、用量计量、按 attempt 存产出都已经接好了，所以这是把沙盘变成产品的那一刀。假 worker 保留 —— 测试依赖它离线且确定。Worker 接口需要扩展以支持工具调用（tool-call/tool-result 循环），这是阶段 1 最核心的接口改动。
- **API key 的配置与存储。** 本地配置文件（`~/.agent-world/config.json`），权限 600，不进仓库。UI 设置面板可读写。
- **让质检标准真正生效。** `gate.criterion` 在 schema 里有，但 inspector 没有输入框，`fakeWorker.judge` 也不读它，只按固定 attempt 数放行。需要：inspector 加字段 + 真实 judge 读它。
- **提示词编辑要能存活。** 现在改了 prompt 只在派发时才 `PUT /api/graphs/:id`，不跑就刷新会丢。改为 debounce 自动保存。
- **`halt` 要能恢复。** 停线等人工现在是终态，没有"我看过了，继续"的命令。事件流能表达，缺的是命令和 UI。
- **真实调用的失败处理。** 错误分类（超时、限流、provider 报错、认证失败），节点级重试策略（指数退避，重试不增加 attempt），超时控制，结构化失败面板。
- **事件 schema 预留字段。** `packet.sent` 加可选 `artifactId`/`metadata`，`node.failed` 加可选 `errorCode`，`Usage` 加可选 `cachedTokens`，source 节点加可选 `connector` config。都是加法，bump 版本不需要。

**退出条件：** 用真实模型跑通一条对自己有用的产线（例如"写草稿 → 批评 → 改写 → 输出"），失败时能看懂为什么失败，改完 prompt 刷新不丢。

**明确不做：** 并行、模板、技能卡 UI、多图管理、Connector 实现、MemoryBackend。

详细任务拆解见 [docs/roadmap-tasks.md](docs/roadmap-tasks.md) 阶段 1。

## 阶段 2 —— 产线表达力

**目标：** 能表达真实工作流，而不只是一条直线。

**任务：**

- **并行分支。** 编译器现在输出线性拓扑序，两座互不依赖的厂房是一前一后跑而不是同时跑。plan 的结构能容纳分层，引擎的 cursor 才是假设单线程的那一处。改为按 levels 并发执行 + 信号量限流。
- **多入边汇合的调度。** 数据层面已经能合并（`inputFor` 把所有入边产出拼起来），缺的是调度层面等齐所有上游（barrier）。
- **技能卡。** `agent.skills` 现在是个字符串数组，没有 UI。这是可装备技能卡落地的地方 —— 工具卡、prompt 模块卡、输出契约卡、质检卡，无解锁成本。内置工具注册表 + 装备 UI。
- **多产线管理。** 数据库的 `graphs` 表和 `/api/graphs` 列表接口都在，但前端只加载 `seed` 这一张图。需要产线列表、新建、复制、命名、删除。
- **产线模板**，把几条常用结构做成起手式。第一个重点模板：商品详情页生产。
- **节点级预算**，单个厂房花费上限。

**退出条件：** 能不改代码地搭出一条有并行、有汇合、有多个质检点的产线，并存成模板复用。

## 阶段 3 —— 可信运行

**目标：** 跑长任务不心慌。出问题能定位，花了多少钱能对账。

**任务：**

- 运行历史列表与详情，不只有当前这一次
- 成本报表：按产线、按厂房、按 attempt、按时间拆分，回答"钱花在哪"
- 长跑的断线重连（SSE 已支持从 seq 续传，需要前端在断开后自动重连而非静默停住）
- 节点级重试策略完善：与返工环明确区分开 —— 返工是质检驳回（增加 attempt），重试是调用失败（不增加 attempt）
- 结构化的失败面板，取代现在顶部那条红色横幅
- 成本预警（到 80% 提醒，到 100% 跳闸）
- Packet/Artifact 分层正式落地：产出可以是文件、图片、结构化数据，不只文本
- 评估体系雏形：合格率、返工率、成本趋势

**退出条件：** 一条跑十几分钟的产线中途断网，恢复后状态正确、事件不丢不重；跑完能说清这次花了多少钱、花在哪个环节。

## 阶段 4 —— 开源准备

**目标：** 陌生人能 clone 下来跑起来，并且能加自己的 worker。

**任务：**

- Worker 插件化：从"改代码换实现"变成"按接口放一个文件"
- Connector：定义接口，实现文件 connector 和 HTTP API connector
- MCP 客户端：MCP server 的工具自动成为技能卡
- Artifact 存储抽象：本地文件系统 → S3 兼容
- 多模态支持：图片/文件作为原料
- 触发方式：Webhook、定时、事件链、批量
- 人机协作增强：人工编辑产出、审批节点、通知
- 示例产线若干，覆盖典型场景
- 文档：架构说明、扩展指南、贡献指南
- 密钥泄漏检查、许可证、CI、Docker Compose
- 首次启动引导，替代现在写死的 seed 图

**退出条件：** 一个没读过这份文档的人，能在十分钟内跑起来并接上自己的 provider。

## 阶段 5 —— 商业化

只记方向，不细化 —— 到那时前四阶段的产物会改变判断。

- 多租户与数据隔离、鉴权（RBAC/SSO）、真实计费与配额（阶段 0 的事后计量和预算上限是这件事的地基）
- 云端部署（PostgreSQL + Redis + 对象存储 + K8s）、团队协作与产线共享
- Knowledge/MemoryBackend：向量检索、档案室、复盘报告与改进建议（人决策，系统不自动改）
- 企业 Connector 套件、审计日志导出
- 产线版本管理、A/B 测试、回归测试集

**现在唯一要做的事是不堵死它：** worker 可替换、事件流带版本号、成本计量真实。这三条已经在了。

## 已知的风险

- **游戏化可能盖过可用性。** 卡车、电表、厂房是隐喻，不是目的。任何时候如果隐喻开始妨碍看清"这条产线到底在干什么"，改隐喻。
- **返工环的表达力上限。** 现在一个质检站管一条返工线、回到某个上游厂房。嵌套返工、多质检站互相返工都还没验证过。等真实工作流撞上来再改，不预先设计。
- **阶段 1 的 provider 接入可能反过来改动模型层。** 真实流式响应、工具调用、多轮对话都可能撑不进现在的 `node.delta` + 单次产出结构。这是接入时才能知道的事，任务路线图里已把 Worker 接口扩展列为阶段 1 首要任务。
